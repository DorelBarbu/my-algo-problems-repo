#define INPUT_FILE "date.in"
#define OUTPUT_FILE "date.out"

#include <iostream>
#include <fstream>
#include <string>

using namespace std;

ifstream in(INPUT_FILE);
ofstream out(OUTPUT_FILE);

int main() {
  
  return 0;
}